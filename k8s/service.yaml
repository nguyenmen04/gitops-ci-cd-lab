apiVersion: v1
kind: Service
metadata:
  name: gitops-web
  namespace: default
spec:
  selector:
    app: gitops-web
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
