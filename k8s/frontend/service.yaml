apiVersion: v1
kind: Service
metadata:
  name: gitops-web
  namespace: default
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: gitops-web
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
